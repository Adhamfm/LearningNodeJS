<!DOCTYPE html>
<html>


<%- include('./partials/head.ejs') %>

<body>
    <%- include('./partials/nav.ejs') %>

    <h1>Welcome to Parrot Blog</h1>
    <p>This is a basic HTML webpage example.</p>
    <h2>Features:</h2>
    <ul>
        <li>HTML</li>
        <li>CSS</li>
        <li>JavaScript</li>
    </ul>
    <p>This webpage includes some basic features like headings, paragraphs, images, and lists.</p>

    <div class="blogs content">
        <h2>All Blogs</h2>
        <% if (blogs.length > 0) { %>
            <% blogs.forEach(blog => { %>
                <h3 class="title"> <%= blog.title %> </h3>
                <p class="snippet"> <%= blog.snippet %> </p>
            <% }) %>
        <% } else {%>
            <p>There are no blogs to display</p>
        <% } %>
    </div>

    <%- include('./partials/footer.ejs') %>
</body>

</html>